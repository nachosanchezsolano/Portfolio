# Applications

The target application boundaries are:

- `web`: Astro portfolio.
- `api`: FastAPI HTTP application.
- `worker`: Python process for asynchronous indexing jobs.

The Astro frontend lives under `apps/web/`; the FastAPI application remains under `api/` until its phase 1 boundary is implemented.
